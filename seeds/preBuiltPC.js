const  PrebuiltPC = require('../models/prebuiltPC');

const pcBuilds = [
    {
        build_type:
        monitor:
        cpu:
        storage:
        ram:
        motherboard:
        chipset:
        gpu:
        psu:
        os:
        case:
    }
]

const seedPrebuiltPC = () => Monitor.bulkCreate(monitorData);

module.exports = seedMonitor;